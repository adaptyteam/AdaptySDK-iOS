
# Module Overview

UI Builder for AdaptyUI SDK. 
Build UI from AdaptyUIConfiguration and AdaptyUIState.

# Code Formatting

SwiftFormat is configured via `.swiftformat`. Key settings:
- 4-space indentation
- LF line breaks
- Import grouping: testable-last